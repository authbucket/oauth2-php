{% extends "html.html.twig" %}

{% block page %}
<div class="container">
    <p>Access Token: {{ access_token }}</p>
    <p><a class="btn btn-primary" role="button" href="{{ resource_path }}">Get Protected Resource</a></p>
    <p><a href="{{ path('index') }}">Return</a></p>
</div>
{% endblock %}
